# Test generated by RoostGPT for test python-days using AI Type Vertex AI and AI Model codechat-bison-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    csv: test_Get_day_00fe196675-Test.csv
    skipHeader: true
  expect:
    - expect:
        statusCode: 200
        body:
          hasProperty: day
        headers:
          Content-Type: application/json
      scenario: Successful API request
    - expect:
        statusCode: 404
      scenario: API request with validation failure
    - expect:
        statusCode: 200
        body:
          hasProperty: day
        headers:
          Content-Type: application/json
      scenario: API request with edge case

