# Test generated by RoostGPT for test python-days using AI Type Vertex AI and AI Model codechat-bison-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - scenario: Successful API request
    payload:
      name: John Doe
      age: 30
    headers:
      Content-Type: application/json
    expect:
      statusCode: 201
      body:
        type: JSON
        hasProperty: success
        hasProperty: message
  - scenario: API request with validation failure
    payload:
      name: John Doe
    headers:
      Content-Type: application/json
    expect:
      statusCode: 400
      body:
        type: JSON
        hasProperty: success
        hasProperty: message
  - scenario: API request with edge case
    payload:
      name: John Doe
      age: 30.5
    headers:
      Content-Type: application/json
    expect:
      statusCode: 201
      body:
        type: JSON
        hasProperty: success
        hasProperty: message
  - scenario: Validation failure with invalid URL
    request:
      url: http://localhost:5000/invalid-url
    expect:
      statusCode: 404
      body:
        type: JSON
        hasProperty: success
        hasProperty: message

